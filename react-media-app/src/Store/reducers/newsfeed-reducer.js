const initialState = {
  loading: false,
  authError: null,
};

const newsfeedReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default newsfeedReducer;